<UserControl x:Class="accountability_contacts.contact_form"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:accountability_contacts"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0">Addresses</Label>
        <Label Grid.Row="0" Grid.Column="1">Emails</Label>
        <Label Grid.Row="0" Grid.Column="2">Phone Numbers</Label>
        <ListBox x:Name="Addresses"  Grid.Row="1" Grid.Column="0"></ListBox>
        <ListBox x:Name="Emails" Grid.Row="1" Grid.Column="1"></ListBox>
        <ListBox x:Name="Numbers" Grid.Row="1" Grid.Column="2"></ListBox>
    </Grid>
</UserControl>
